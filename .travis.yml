dist: precise

language: php

php:
  - 7.3
  - 7.4

services:
  - sqlite

before_script:
  - cp .env.testing .env
  - composer self-update
  - composer install --no-interaction
  - php artisan migrate

script:
  - vendor/bin/phpunit
